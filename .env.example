# The application is read-only. No wallet key belongs here.
CLUSTERMAP_HOST=127.0.0.1
CLUSTERMAP_PORT=8766
CLUSTERMAP_SNAPSHOT=data/curator_snapshot.json.gz

# Optional display context. Leave unset to show "USD unavailable".
# CLUSTERMAP_ETH_USD=4200.00

